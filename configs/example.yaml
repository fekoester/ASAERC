processed_dir: "processed_data"
input_dim: 4
output_dim: 4
train_frac: 0.8

Nx: 100
Ny: 100
steps_per_input: 25
n_injection_points: 500
n_measurement_points: 128
use_injection_as_sensors: false

mode: "diffusion"
boundary: "periodic"
c: 0.0
D: 0.000097
gamma: 0.0
forcing_type: "constant"

dx: 0.0010101
dy: 0.0010101
dt: 0.0023667

hidden_dim: 128
n_queries: 128
static_positions: false
use_linear_model: false

lr: 0.01
final_lr: 0.0001
epochs: 500
batch_size: 512
repulsion_coef: 0.0

seed: 0
save_dir: "runs"
save_corr: true
